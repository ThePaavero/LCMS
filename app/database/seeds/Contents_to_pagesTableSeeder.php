<?php

class Contents_to_pagesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('contents_to_pages')->truncate();

		$contents_to_pages = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('contents_to_pages')->insert($contents_to_pages);
	}

}
